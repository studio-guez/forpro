name: CI / Deploy

on:
  push:
    branches: [main, preprod]
    tags: ['v*']
  pull_request:
    branches: [main, preprod]
  workflow_dispatch:
    inputs:
      target:
        description: 'Deployment target'
        required: true
        default: preprod
        type: choice
        options: [preprod, production]

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    name: Lint, build & test
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    concurrency:
      group: ci-${{ github.ref }}
      cancel-in-progress: true

    env:
      APP_ENV: testing
      APP_DEBUG: 'true'
      DB_CONNECTION: sqlite
      DB_DATABASE: ${{ github.workspace }}/database/database.sqlite
      CACHE_STORE: array
      SESSION_DRIVER: array
      QUEUE_CONNECTION: sync
      MAIL_MAILER: array

    steps:
      - uses: actions/checkout@v5

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: bcmath, exif, gd, intl, mbstring, pcntl, pdo_sqlite, zip
          coverage: none
          tools: composer:v2

      - name: Setup Node 20
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Cache Composer
        uses: actions/cache@v5
        with:
          path: ~/.cache/composer
          key: composer-${{ runner.os }}-${{ hashFiles('composer.lock', 'composer.json') }}
          restore-keys: composer-${{ runner.os }}-

      - name: Install PHP dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Install Node dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Prepare environment
        run: |
          cp .env.example .env
          mkdir -p database
          touch database/database.sqlite
          php artisan key:generate
          php artisan migrate --force

      - name: Build frontend assets
        run: npm run build

      - name: Lint (Pint, dry-run)
        run: vendor/bin/pint --test

      - name: Run tests
        run: php artisan test

  # ---------------------------------------------------------------------------
  # Single build job, shared by both environments. Skipped on pull requests.
  # ---------------------------------------------------------------------------
  build-image:
    name: Build & push image
    needs: [test]
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
      release_id: ${{ steps.meta.outputs.release_id }}
      target: ${{ steps.meta.outputs.target }}
    steps:
      - uses: actions/checkout@v5

      - name: Resolve deployment target and tags
        id: meta
        run: |
          SHA7=${GITHUB_SHA::7}
          TS=$(date -u +%Y%m%dT%H%M%SZ)

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TARGET="${{ inputs.target }}"
          elif [ "${GITHUB_REF}" = "refs/heads/preprod" ]; then
            TARGET="preprod"
          else
            TARGET="production"
          fi

          if [ "$TARGET" = "preprod" ]; then
            IMAGE_TAG="preprod-sha-$SHA7"
            FLOATING_TAG="preprod"
          else
            IMAGE_TAG="sha-$SHA7"
            FLOATING_TAG="latest"
          fi

          echo "target=$TARGET"             >> "$GITHUB_OUTPUT"
          echo "image_tag=$IMAGE_TAG"       >> "$GITHUB_OUTPUT"
          echo "floating_tag=$FLOATING_TAG" >> "$GITHUB_OUTPUT"
          echo "release_id=$TS-$SHA7"       >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure private Composer auth (optional)
        env:
          COMPOSER_AUTH_JSON: ${{ secrets.COMPOSER_AUTH }}
        run: |
          if [ -n "$COMPOSER_AUTH_JSON" ]; then
            printf '%s' "$COMPOSER_AUTH_JSON" > ./auth.json
          fi

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/prod/Dockerfile
          push: true
          provenance: false
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.image_tag }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.floating_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            WWWUSER=33
            WWWGROUP=33

      - name: Package compose bundle
        run: tar -czf compose-bundle.tgz docker/compose docker/prod .env.example public/robots.txt public/.htaccess

      - uses: actions/upload-artifact@v6
        with:
          name: compose-${{ steps.meta.outputs.release_id }}
          path: compose-bundle.tgz
          retention-days: 7

  # ---------------------------------------------------------------------------
  # Preprod deploy.
  # ---------------------------------------------------------------------------
  deploy-preprod:
    name: Deploy to preproduction
    needs: build-image
    if: needs.build-image.outputs.target == 'preprod'
    runs-on: [self-hosted, filogie, preprod, docker]
    timeout-minutes: 15
    environment: preprod
    concurrency:
      group: deploy-preprod
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5
      - uses: actions/download-artifact@v7
        with:
          name: compose-${{ needs.build-image.outputs.release_id }}
          path: /tmp

      - name: Deploy to preproduction
        uses: ./.github/actions/deploy
        with:
          deploy_path:          ${{ secrets.DEPLOY_PATH }}
          ghcr_user:            ${{ secrets.GHCR_PULL_USER || github.actor }}
          ghcr_token:           ${{ secrets.GHCR_PULL_TOKEN }}
          image_tag:            ${{ needs.build-image.outputs.image_tag }}
          release_id:           ${{ needs.build-image.outputs.release_id }}
          bundle_path:          /tmp/compose-bundle.tgz
          compose_project_name: ${{ secrets.COMPOSE_PROJECT_NAME }}

  # ---------------------------------------------------------------------------
  # Production deploy.
  # ---------------------------------------------------------------------------
  deploy-production:
    name: Deploy to production
    needs: build-image
    if: needs.build-image.outputs.target == 'production'
    runs-on: [self-hosted, filogie, production, docker]
    timeout-minutes: 15
    environment: production
    concurrency:
      group: deploy-production
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5
      - uses: actions/download-artifact@v7
        with:
          name: compose-${{ needs.build-image.outputs.release_id }}
          path: /tmp

      - name: Deploy to production
        uses: ./.github/actions/deploy
        with:
          deploy_path:          ${{ secrets.DEPLOY_PATH }}
          ghcr_user:            ${{ secrets.GHCR_PULL_USER || github.actor }}
          ghcr_token:           ${{ secrets.GHCR_PULL_TOKEN }}
          image_tag:            ${{ needs.build-image.outputs.image_tag }}
          release_id:           ${{ needs.build-image.outputs.release_id }}
          bundle_path:          /tmp/compose-bundle.tgz
          compose_project_name: filogie

