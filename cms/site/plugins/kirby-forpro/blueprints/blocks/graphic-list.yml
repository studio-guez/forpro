title: Liste Graphique

icon: list-bullet

wysiwyg: false
preview: fields

fields:

  items:
    label: Éléments de la liste
    type: structure
    min: 1
    fields:
      title:
        label: titre
        type: writer
        inline: true
        marks:
          - italic
      scrollToBlock:
        type: number
        label: Nombre de bloc vers lequel scroller
        info: Si renseigné, le clic sur l'élément de la part de l'utilisateur·icexs fera scroller la page vers le nombre de blocs renseignés
