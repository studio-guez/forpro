<script>
/**
 * Preview of a `restaurantfiles` value inside structure and object tables.
 * Identical to the preview of Kirby's own files field, the picker payload
 * carries the same `filename` / `link` / `image` keys.
 */
export default {
    extends: "k-files-field-preview",
};
</script>
