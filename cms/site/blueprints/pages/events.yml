title:
  en: Events
  fr: Événements

extends: pages/core-page

icon: calendar

tabs:
  content:
    label:
      en: Content
      fr: Contenu
    icon: text
    sections:
      meta:
        type: fields
        fields:
          title:
            label:
              en: Title
              fr: Titre
            type: text
            required: true
      events:
        extends: sections/child-pages
        label:
          en: Events
          fr: Événements
        templates:
          - event
        create: event
      extra:
        type: fields
        fields:
          body:
            extends: fields/bodyExtra
            # The agenda module would just duplicate the listing above.
            fieldsets:
              module-agenda: false

  seo: tabs/seo
