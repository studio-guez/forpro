title:
  en: Event
  fr: Événement

extends: pages/event-project-base

status:
  draft: true
  listed: true

num: zero

options:
  preview: "{{ page.frontendUrl }}"

tabs:
  content:
    columns:
      sidebar:
        sections:
          dates:
            type: fields
            fields:
              sepDates:
                type: line
              dateStart:
                label:
                  en: Start date
                  fr: Date de début
                type: date
                time: true
                required: true
              dateEnd:
                label:
                  en: End date
                  fr: Date de fin
                type: date
                time: true
          taxonomies:
            type: fields
            fields:
              sepTaxonomies:
                type: line
              domains:
                extends: fields/taxonomies/domains
              eventThemes:
                extends: fields/taxonomies/event-themes
