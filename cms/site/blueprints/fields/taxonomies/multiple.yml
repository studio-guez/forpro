# Generic base for selecting multiple taxonomy terms.
# Per-taxonomy variants extend this and set `options.query`.
type: tags
accept: options
options:
  type: query
  text: "{{ page.title }}"
  value: "{{ page.slug }}"
