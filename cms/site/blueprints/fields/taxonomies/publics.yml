extends: fields/taxonomies/taxonomy
label:
  en: Publics
  fr: Publics
options:
  # `index` instead of `children`: publics is a two-level taxonomy and sub-terms
  # have to be selectable at the same level as their parent.
  query: site.find("taxonomies").find("publics").index.listed
